// Custom diagnostic codes
const CUSTOM_CODES = {
    UNMATCHED_PAREN: "unmatched-paren",
    UNMATCHED_BOLD: "unmatched-bold",
};

// Centralize config here
const CONFIG = {
    PYTHON_PATH: "python"
};

module.exports = { CUSTOM_CODES, CONFIG };
